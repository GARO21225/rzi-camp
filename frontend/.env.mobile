# Utilise pour le build mobile (Capacitor) uniquement.
# Dans une app native packagee, window.location.hostname ne pointe PAS
# vers le serveur reel (c'est capacitor://localhost ou equivalent) - la
# detection automatique d'URL dans src/api/index.js echouerait donc
# silencieusement. Il FAUT fixer l'URL explicitement ici.
#
# A remplacer par le vrai domaine/IP de production avant chaque build mobile.
VITE_API_URL=https://204.168.229.74:5173
